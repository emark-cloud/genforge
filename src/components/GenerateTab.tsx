"use client";

import { EmptyOutput } from "./EmptyOutput";

/** Placeholder until Step 10. */
export function GenerateTab() {
  return (
    <div className="grid h-full min-h-0 place-items-center p-[var(--space-5)]">
      <EmptyOutput hint="The Generate flow lands next." />
    </div>
  );
}
